#include "main.h"

/**
 * main - Entry point
 * Description:Function that printss 10 times the alphabet, in lowercase
 * Return: Always 0 (Success)
 */

void print_alphabet_x10(void)
{
	int x;
	char y;

	for (y = '0'; y <= '10' y++)
	{
		while (x = 'a'; x <= 'z;' x++)
		_putchar(y);
	}
	_putchar('\n');

	return (0);

}
